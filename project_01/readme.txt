The projects are saved in two Jupyter notebooks:
- Animation contains the Gapminder animation
- Smaller Projects contains everything else
The sources are read from the data directory, and the outputs are saved in the root
animation-frames is a temporary directory for storing the animation frames